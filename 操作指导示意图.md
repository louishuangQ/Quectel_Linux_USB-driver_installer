# Quectel 驱动自动安装 · 操作指导示意图

颜色图例（对应下方流程图）：

| 颜色 | 含义 |
|---|---|
| 🟨 黄色 | 用户手动操作 |
| 🟦 蓝色 | 脚本自动执行 |
| ⬜ 灰色 | 发布侧一次性（每内核版本一次） |
| 🟩 绿色 | 完成 / 结果 |

> 目标：把「插入 Quectel 模组 → 驱动安装 → 拨号上网」这条链路上**所有能自动化的步骤都交给脚本**，用户只在少数几个关键节点介入。

```mermaid
flowchart TD
    %% ================= 发布侧（一次性） =================
    subgraph PUB["发布侧 · 一次性（每个内核版本一次）"]
        A1["发布源码包<br/>./make_source_assets.sh<br/>上传 quectel-src-vX.Y.Z.tar.gz"] --> A2["可选：发布预编译包<br/>sudo ./make_release_asset.sh<br/>上传 quectel-usb-&lt;uname -r&gt;-&lt;arch&gt;.tar.gz"]
    end

    %% ================= 首次安装 =================
    subgraph FIRST["🔧 首次安装（用户发起一次，其余自动）"]
        B1["执行<br/>sudo ./quectel_auto_install.sh --first-time<br/>[--prebuilt-repo owner/repo]"] --> B2["检测 Quectel 模组<br/>(VID 2c7c / 05c6)"]
        B2 --> B3["校验内核版本是否 5.18.x"]
        B3 --> B4{"内核配置<br/>CONFIG_USB_SERIAL 已启用?"}
        B4 -- "否" --> B5["重新编译内核 / 启用配置<br/>后重试"]
        B5 -.-> B1
        B4 -- "是" --> B6{"驱动已安装?"}
        B6 -- "是" --> B12["modprobe 加载驱动"]
        B6 -- "否" --> B7{"有匹配的<br/>预编译 .ko 包?"}
        B7 -- "是" --> B8["下载 + 校验 vermagic + 安装 .ko<br/>（免编译，无需 gcc/headers）"]
        B7 -- "否" --> B9["安装内核头文件<br/>linux-headers-&lt;uname -r&gt;"]
        B9 --> B10["按需下载匹配源码包<br/>quectel-src-vX.Y.Z.tar.gz + 编译"]
        B8 --> B12
        B10 --> B12
        B12 --> B13["安装启动器到 /usr/local/bin<br/>安装 udev 规则 + 注册 DKMS"]
        B13 --> B14["校验 /dev/ttyUSB* 与 wwanX"]
        B14 --> B15["设备已就绪"]
    end

    %% ================= 日常使用 =================
    subgraph HOT["日常使用 · 插入模组（全自动）"]
        C1["插入 Quectel 模组"] --> C2["udev 规则自动触发<br/>--quick-load"]
        C2 --> C3{"驱动已安装?"}
        C3 -- "是" --> C4["modprobe 加载<br/>（约 2~3 秒）"]
        C3 -- "否" --> C5{"有匹配的<br/>预编译 .ko 包?"}
        C5 -- "是" --> C6["下载 + 安装 + 加载<br/>（免编译）"]
        C5 -- "否" --> C7["写日志，提示执行一次<br/>--first-time"]
        C4 --> C8["设备已就绪（可拨号）"]
        C6 --> C8
        C7 -.-> C8
    end

    %% ================= 拨号 =================
    subgraph DIAL["拨号上网（用户发起）"]
        D1["执行<br/>sudo quectel-CM -s &lt;APN&gt; &amp;<br/>（QMI/MBIM）或 pppd call quectel-ppp"] --> D2["获取 IP/DNS，开始上网"]
    end

    C8 --> D1

    %% ================= 卸载 =================
    subgraph UN["卸载（用户发起）"]
        E1["执行<br/>sudo ./quectel_auto_install.sh --uninstall"] --> E2["卸载模块 / udev 规则 / DKMS / 启动器"]
    end

    %% 样式
    classDef user fill:#fff3cd,stroke:#f0ad4e,stroke-width:2px,color:#333
    classDef auto fill:#d1ecf1,stroke:#17a2b8,stroke-width:2px,color:#333
    classDef pub  fill:#e2e3e5,stroke:#6c757d,stroke-width:2px,color:#333
    classDef done fill:#d4edda,stroke:#28a745,stroke-width:2px,color:#333

    class A1,A2 pub
    class B1,B5,B9,D1,E1 user
    class B2,B3,B4,B6,B7,B8,B10,B12,B13,B14,C2,C3,C4,C5,C6,C7,E2 auto
    class B15,C8,D2 done
```

## 各步骤归属速览

| 阶段 | 用户需要做的事 | 脚本自动做的事 |
|---|---|---|
| 发布侧（一次性） | 跑 `make_source_assets.sh` 上传源码包（可选再跑 `make_release_asset.sh` 上传预编译包） | — |
| 首次安装 | 执行一次 `--first-time --prebuilt-repo <repo>`；源码路径需先装 `linux-headers` | 检测模组 → 版本/配置校验 → 下载预编译包（或按需下载源码+编译）→ 安装 → 加载 → 装 udev/DKMS → 校验 |
| 日常使用 | 插入模组（唯一动作） | udev 触发 `--quick-load` → 下载预编译包（若有）或 modprobe → 驱动就绪 |
| 拨号上网 | 执行 `quectel-CM -s <APN>`（或 `pppd`） | — |
| 卸载 | 执行 `--uninstall` | 卸载模块 / udev / DKMS / 启动器 |

## 说明

- **统一收敛目标**：脚本无论走哪条路径，最终都以**「设备已就绪」**或**「设备未就绪 + 原因」**收尾——环境已 OK 就直接报告就绪，环境不 OK 就一直往下装（下载预编译包或源码编译 → 加载 → 再校验），直到就绪或给出明确阻塞原因。
- **最省心路径**：发布侧上传好预编译包后，目标机上「插设备 → 驱动自动装好」，用户只需再执行一条 `quectel-CM` 拨号命令。
- **无预编译包时**：脚本按需下载匹配源码包再编译；首次插入会提示执行一次 `--first-time`，之后即恢复全自动。
- 预编译 `.ko` 与「内核版本 + 架构 + 配置」强绑定，仅适用于版本/配置固定的嵌入式/BSP 内核。
